Fix 03.07.22


Файл array_ptr.h

1) строка 13

    explicit ArrayPtr(size_t size) {
        if (size == 0) {
            ArrayPtr();
 
Переработан конструктор. Проверка на size == 0 возвращена, без вызова конструктора

2) строка 23

    ArrayPtr(ArrayPtr&&);

Реализация конструктора сокращена с помощью std::exchange

3) строка 38

    ArrayPtr& operator=(ArrayPtr&& other)

Реализация метода сокращена с помощью std::exchange


Файл simple_vector.h

4) строка 43

    explicit SimpleVector(size_t size) : 
        size_(size), capacity_(size), items_(ArrayPtr<Type>(size)) {

Во всех конструкторах вектора убран конструктор ArrayPtr<Type>(size);

5) строка 65

     SimpleVector(SimpleVector&& other) noexcept

Конструктор оптимизирован с использованием std::exchange

6) строка 70

    SimpleVector& operator=(const SimpleVector& rhs)

Оператор оптимизирован

7) строка 78

    SimpleVector& operator=(SimpleVector&& rhs) 

Добавлен оператор премещения для SimpleVector

8) строка 95 и 101

    Type& operator[](size_t index) noexcept {
        assert(index >= 0);
 
Убраны ассерты на Index >= 0 